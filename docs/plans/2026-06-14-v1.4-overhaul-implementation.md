# v1.4 출석부 대규모 개선 — 구현 계획

작성: 2026-06-14 · 브랜치: `feat/v1.4-attendance-overhaul`

## 검증 제약 (중요)
- 이 작업 환경에서는 **실제 Google 시트 / 배포된 GAS / 카카오톡 인앱 브라우저 로그인을 실행·테스트할 수 없다.**
- 가능한 검증: `node scripts/check-web.mjs` (JSX 문법), 코드 리뷰, 요구사항 대조.
- 백엔드(GAS)·시트 스키마 변경·실기기 로그인은 **사용자가 재배포/마이그레이션 실행 후 직접 검증**해야 한다.
- 완료 보고는 (a) 변경+문법검증 완료 / (b) 사용자 조치·검증 필요 로 구분한다.

## 사용자 확정 결정
- 새가족#2/#3: **별도 '새가족부' 탭 신설 (전면 재구성)** 채택.
- #3(STUDENTS vs 2026반편성)·#4(교사 탭 2개): **두 번째 교사 탭 정보를 사용자가 제공하면 반영** → 정보 수신 전까지 보류, 최종 보고에서 요청.

## 단계 (각 단계: check-web.mjs 통과 → 커밋)

### A. 이미 충족된 항목 확인 (편집 없음)
- #7 일반교사 = 출석/현황/기도제목만: 현재 base 탭 3개 + 권한 게이트로 이미 충족. 확인만.
- 새가족#5 4주 교육 주차 표기: `setNewcomerProgress` + 1~4주 체크 UI 이미 존재. 확인/보강.

### B. 프론트엔드 전용 (JSX 검증 가능)
- #0 카카오톡 인앱 브라우저: GIS가 인앱 웹뷰에서 차단됨 → 인앱 감지 후 "외부 브라우저로 열기" 배너/버튼.
- #2 결석 사유 필수: `EtcInputSheet` ABSENT일 때 빈 값이면 저장 차단 + 안내. 배치/undo 경로도 점검.
- #6 메뉴 상단 이동: `.bottomnav` → 상단 탭바.
- #9 '황규일 반' → '황규일교사 반': appbar 등 반 라벨 일괄.
- #10 주 라벨 클릭 → 달력(date picker)로 주일 이동.
- 새가족#1 생년월일 연도 별도 선택.
- 새가족#4 등반자 배경색 구분.
- 관리자#6 반별 통계 타일 3열 그리드.
- 관리자 통계 그래프: 예배별 막대(#2), 주별 학년별 막대(#4), 주별/월별 합계 선형, 교사 출석현황.

### C. 프론트+백엔드 결합 (GAS 문법만 검증)
- #1 기도제목 응답 여부: PRAYERS에 `응답`/`응답일` 컬럼 추가, 체크 UI. 날짜는 이미 표시됨.
- #8 새가족부 교사 = 새가족부(통계)+기도제목만: 역할별 탭 커스터마이즈, newcomer_staff에 새가족 통계 노출.
- #11 명단 전화 아이콘: ROSTER_FIELDS에 연락처 추가, 카드 우측 tel: 아이콘.
- #5 / 관리자#5 학생 정보 수정: 신규 `updateStudent` 엔드포인트(본인 반/관리자 권한) + 수정 UI. 현재 쓰기 엔드포인트 없음(설계상 시트 직접) → 가장 무거움.
- 관리자#1~4 통계: getAllAttendance 기반 클라이언트 집계(예배별/반별 결석사유/주별 학년별).

### D. 새가족부 전면 재구성 (마지막, 독립)
- 새 '새가족부' 시트 탭 + 마이그레이션 함수.
- addNewcomer → 새가족부 탭 기록. 등반 전 출석도 새가족부 기준.
- graduate → '2026 반편성'으로 복사 + 교사명 기재.
- 명단에 등반 교사반 선택, 등반자 배경색.

### E. 검토 산출물 (코드 아닌 문서)
- #3 STUDENTS vs 2026반편성 권고 문서.
- #4 두 번째 교사 탭: 사용자 정보 수신 후 반영(보류).
